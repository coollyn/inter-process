# inter-process
communication between parent and child process using c++, named pipe

use double pipes to timely communicate between parent and child processes simutaneously
parent/child processes use WaitForSingleObject to mutualy detect exiting status of each other
